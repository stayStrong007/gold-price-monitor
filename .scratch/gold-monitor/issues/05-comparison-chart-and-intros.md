# 05 · 多线对比折线图（含切片纯函数 sliceForChart，测试接缝 B）+ 品牌简介

Status: ready-for-agent

## 切片目标（tracer bullet）

完成首页最后两段：

1. **多线对比折线图** —— 顶部复选框选线（默认勾黄金9999）× 时间区间切换（30/90/180 天）。
2. **品牌简介** —— 页面底部纯文字无序列表，手写静态文本。

并 TDD 实现支撑图表的纯函数 `sliceForChart(data, days, selectedNames) → 待渲染数据`（PRD 测试接缝 B），与 ECharts 渲染解耦。

## 完成的样子（可亲眼验证）

- 折线图随复选框增删线；默认勾选 9999。
- 切 30/90/180 天，图随之缩放。
- 底部展示 7 个品牌简介（无序列表，手写文本）。
- `sliceForChart` 测试全绿：选 30 天仅返回 30 天；切换选中线仅返回选中项；涨跌幅按「最新 vs 前一天」算。

## 不在本切片

- 部署/Action（→ 06）。

## 备注

- 复选框（选哪些线）与时间区间（看多长）是两个独立控件，叠加生效。
- 纯函数测试不触网、不依赖 ECharts；ECharts 渲染本身按 PRD 属 Out of Scope。
