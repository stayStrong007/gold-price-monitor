# 03 · 每日更新脚本（薄壳：抓取 → 调 mergeGoldData → 写回）

Status: ready-for-agent

## 切片目标（tracer bullet）

新建每日更新脚本，作为切片 02 那个纯函数外面的**薄壳**：读取已有 `data/gold_prices.json` → 只抓增量 → 调用 `mergeGoldData` → 写回。

端到端细线：**已有数据 + 今天的增量 → 更新后的同一个 JSON 文件**。

## 完成的样子（可亲眼验证）

- 在已有 01 产出的 JSON 基础上运行脚本，文件被更新（含到今天的数据），历史未丢失。
- 品牌价：直接拿接口整段交给 `mergeGoldData` 合并。
- 黄金9999：只抓「数据中最后一天 → 今天」这几天的 HTML（通常 1 天）。
- 与 01 的一次性脚本**并存**，分工明确：01 引导首版，03 每日增量。

## 不在本切片

- 合并逻辑本身（已在 02，本切片只调用）。
- GitHub Action 自动化（→ 06）。
- 前端。

## 备注

- 抓取部分（网络 I/O）按 PRD 属 Out of Scope，不写自动化测试，靠手动跑验证；脚本的「价值逻辑」已在 02 被测过。
- 失败不做主动报警（PRD 取舍），依赖 Action 非零退出的默认失败通知。
